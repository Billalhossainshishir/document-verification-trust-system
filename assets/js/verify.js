const KEY="trustdoc-demo-v1";const id=new URLSearchParams(location.search).get("id");const el=document.querySelector("#publicRecord");let s={documents:[],attempts:[]};try{s=JSON.parse(localStorage.getItem(KEY))||s}catch{}
const d=s.documents.find(x=>x.verification_id===id);
if(!id)el.innerHTML="<p class='muted'>No Verification ID supplied.</p>";
else if(!d)el.innerHTML="<p><b>"+id+"</b> is not present in this browser's local demo storage.</p><p class='muted'>The full backend exposes GET /api/public/verify/{verification_id} for persistent records.</p>";
else{const a=s.attempts.find(x=>x.verification_id===id);el.innerHTML="<div class='record'><span class='recordstatus'>REGISTERED FINGERPRINT</span><div class='receiptline'><span>Verification ID</span><b>"+d.verification_id+"</b></div><div class='receiptline'><span>Document ID</span><b>"+d.document_id+"</b></div><div class='receiptline'><span>Filename</span><b>"+d.filename+"</b></div><div class='receiptline'><span>Document type</span><b>"+d.document_type+"</b></div><div class='receiptline'><span>Registered</span><b>"+new Date(d.registered_at).toLocaleString()+"</b></div><div class='receiptline'><span>SHA-256</span><span class='mono'>"+d.sha256+"</span></div><div class='receiptline'><span>Latest check</span><b>"+(a?a.status+" · "+new Date(a.verified_at).toLocaleString():"No verification attempts")+"</b></div></div>"}
